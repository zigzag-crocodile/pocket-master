import { TaskType } from './routing'

export function getMockResponse(taskType: TaskType, input: string): string {
  switch (taskType) {
    case 'meeting_task':
      return `## 会议纪要（Mock 模式）

### 会议主题
${input.length > 20 ? input.slice(0, 30) + '……' : '会议内容整理'}

### 核心结论
1. 本次会议明确了核心任务范围和责任分工。
2. 当前目标是保证可运行闭环，而非一次性扩展所有高级能力。
3. 相关负责人需在约定时间前完成各自模块。

### 行动项
| 事项 | 负责人 | 截止时间 | 状态 |
|---|---|---|---|
| 接口开发 | 待确认 | 待确认 | 待完成 |
| 页面开发 | 待确认 | 待确认 | 待完成 |
| 测试验收 | 待确认 | 待确认 | 待开始 |

### 待确认信息
- 具体负责人和截止时间需进一步确认
- 技术方案细节待对齐

> _此结果由 Mock 模式生成，API没接上，先用 Mock 模式帮你跑完整流程。_`

    case 'schedule_task':
      return `## 待办事项（Mock 模式）

- **事项**：${input.slice(0, 40)}
- **建议时间**：根据描述提取（具体时间待确认）
- **地点**：待确认
- **相关对象**：待确认
- **提醒建议**：建议提前 30 分钟确认准备情况，提前 1 天整理相关材料。

> 当前版本仅生成提醒建议，暂不直接写入系统日历。
> _此结果由 Mock 模式生成，API没接上，先用 Mock 模式帮你跑完整流程。_`

    case 'summary_task':
      if (/面试|怎么讲|怎么说|简历/.test(input)) {
        return `## 我在面试中可以这样讲（Mock 模式）

My Pocket Master 是我做的一个轻量化移动端 AI Agent 助手。我把用户日常碎片事务拆成日程、会议、总结、图文 Prompt、资讯提醒等场景，由小当家负责理解任务和调度小帮手。

这个项目的重点是把 Agent 做成可管理的产品对象：每个小帮手都有 AGENTS.md、IDENTITY.md、SOUL.md、MEMORY.md、SKILLS.md、TOOLS.md 配置；用户可以在帮手集市安装，在我的小帮手里测试启用；出现调度错误、配置异常或 API 失败时，修理室里的小诊所会生成诊断报告。

我还设计了当家账本，跟踪任务完成率、调度成功率、模型调用成功率、Mock 触发率、小诊所解决率和导出率，用来衡量理解准确度、结果可用性和商用稳定性。

> _此结果由 Mock 模式生成，API没接上，先用 Mock 模式帮你跑完整流程。_`
      }
      return `## 内容总结（Mock 模式）

### 一句话概括
这是一段用于演示 API 失败后仍能跑通流程的 Mock 总结结果。

### 核心要点
1. 系统已识别任务类型为 summary_task。
2. 小当家尝试调度内容总结小帮手。
3. 由于 API 未配置，系统切换到 Mock 模式并记录到修理室。

### 后续建议
- 检查 DOUBAO_API_KEY 是否正确配置。
- 检查 DOUBAO_MODEL 是否为 doubao-seed-2.0-lite。
- 重新测试模型调用链路。

> _此结果由 Mock 模式生成，API没接上，先用 Mock 模式帮你跑完整流程。_`

    case 'vision_prompt_task':
      return `## 图文识别 Prompt（Mock 模式）

请识别我上传的图片，并按照以下结构输出：

1. **图片主题**：图片的核心主题是什么
2. **关键文字**：图片中出现的重要文字内容
3. **重要元素**：图片中的主要视觉元素
4. **可能含义**：这张图片传达的信息或含义
5. **可执行建议**：基于图片内容，建议采取的下一步行动

> 注意：当前版本只生成 Prompt，不直接处理图片。
> _此结果由 Mock 模式生成，API没接上，先用 Mock 模式帮你跑完整流程。_`

    case 'info_reminder_task':
      return `## 资讯整理（Mock 模式）

### 资讯主题
${input.slice(0, 30)}

### 三条重点
1. 该资讯值得关注，涉及核心业务方向。
2. 相关动态在行业内具有参考价值。
3. 建议在近期跟进最新进展。

### 为什么值得关注
资讯内容与当前工作方向高度相关，可能影响后续决策。

### 建议跟进时间
本周内

### 后续行动建议
- 收藏并标记该资讯
- 与团队分享关键信息
- 安排专项跟进时间

> _此结果由 Mock 模式生成，API没接上，先用 Mock 模式帮你跑完整流程。_`

    case 'repair_task':
      return `## 小诊所诊断报告（Mock 模式）

### 问题类型
调度错误 / 模型失败

### 问题描述
系统在处理当前任务时遭遇异常，小当家已将问题送入修理室。

### 可能原因
1. Doubao API Key 未配置或已失效
2. 小帮手未处于启用状态，无法被调度
3. 任务关键词匹配到了错误的小帮手

### 修复建议
1. 检查 .env 文件中 DOUBAO_API_KEY 是否正确填写
2. 进入"我的小帮手"确认目标小帮手已测试并启用
3. 重新描述任务，使用更明确的关键词

### 是否建议重新测试
是，修复配置后建议重新测试相关小帮手。

### 修复后预期结果
任务可正常调度，模型返回真实结果，不再触发 Mock 模式。

> _此结果由 Mock 模式生成，API没接上，先用 Mock 模式帮你跑完整流程。_`

    default:
      return `## 处理结果（Mock 模式）

小当家已接收你的任务，但 API 未连接，以下为 Mock 演示结果。

你的输入：${input.slice(0, 100)}

> _此结果由 Mock 模式生成，API没接上，先用 Mock 模式帮你跑完整流程。_`
  }
}
